# ABACO DEEP HARNES — Assets placeholder

Aquí vive el branding temporal del clon personalizado de DSH Desktop.

## Archivos
- `logo.svg` — icono cuadrado 480×480 (para app icon, favicon, dock)
- `logo-wordmark.svg` — lockup horizontal con isotipo + texto (para splash, about, web header)

## Paleta
- Fondo: `#0B1020` → `#16204A` (gradient navy)
- Acento primario: `#22D3EE` (cyan) → `#7C3AED` (violet)
- Acento secundario: `#A78BFA`, `#F472B6`
- Texto: `#F8FAFC` (claro), `#0B1020` / `#475569` (oscuro)

## Tipografía
- SF Pro Display (default macOS), fallback a Inter / system-ui
- Bold 700 para "ABACO"
- Medium 500 con tracking +14 para "DEEP HARNES"

## Próximos pasos (no incluidos aquí)
- Convertir `logo.svg` a `.icns` (todos los sizes requeridos por macOS) y `.ico` para Windows
- Generar versiones `@1x`, `@2x`, `@3x` PNG para menubar / tray
- Cuando tengas logo profesional definitivo, reemplazar estos archivos