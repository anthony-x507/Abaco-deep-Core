# ABACO desktop agent-status feature package.
#
# This directory hosts a React/TypeScript indicator that shows "AGENTE
# TRABAJANDO" while the agent is thinking, streaming, or using a tool.
#
# The Python marker file keeps tooling that walks the tree happy when
# the desktop build is vendored inside the larger Python monorepo.
